<%= simple_form_for(@affiliate) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :image_url %>
    <%= f.input :affiliate_url %>
    <%= f.input :affiliate_site %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
