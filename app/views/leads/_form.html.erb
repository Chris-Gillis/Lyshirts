
    <%= simple_form_for(@lead) do |f| %>      
      <div class="well span">
        <%= f.input :email, required: false, as: :string %>
        <%= f.button :submit, 'Submit',  id: 'submit_lead' %>
      </div>
    <% end %>
